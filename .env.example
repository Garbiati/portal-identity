# Copie para .env (gitignored) e ajuste. Em PROD, nada disso fica em arquivo: GCP Secret Manager.

# Admin do console do Keycloak (bootstrap, ambiente local).
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=Admin@dev12345
